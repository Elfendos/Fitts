This project no longer uses Firebase — it was migrated to CloudKit.
This file is obsolete and unused by project.yml; safe to delete manually
from Finder/Explorer if you'd like (this environment couldn't delete it
directly, see ios-native/README.md).
