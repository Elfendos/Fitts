// Moved: this project now uses CloudKit instead of Firebase.
// See CloudKitAccountService.swift for the account/session logic
// (Firebase email-link auth doesn't apply anymore — CloudKit uses the
// device's signed-in iCloud account instead of an explicit login).
